{
  "name": "mandatory-parser",
  "version": "1.0.0",
  "description": "a script that validate an input (check if it has mandatory fields defined in a swagger)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon server.js"
  },
  "author": "ASSABBANE Mehdi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}